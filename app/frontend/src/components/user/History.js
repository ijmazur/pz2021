import React from 'react'
import Core from '../core/Core';

export const History = (props) => {
    return (
        <Core button={props.button} text={"Historia zamówień"} />
      )
}

export default History;
